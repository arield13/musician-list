import React from 'react';

const MainMenu = () => {
  return (
    <div className="main-menu">
      <input type="text" placeholder="Search artist..." className="search-input"/>
    </div>
  );
};

export default MainMenu;
